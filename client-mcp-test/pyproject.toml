[project]
name = "client-mcp-test"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "logfire>=4.2.0",
    "pydantic-ai[logfire]>=0.6.2",
]
